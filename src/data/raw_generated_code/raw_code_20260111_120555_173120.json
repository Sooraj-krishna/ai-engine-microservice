{
  "timestamp": "2026-01-11T12:05:55.173143",
  "language": "javascript",
  "prompt": "\nYou are fixing a accessibility bug in a web application.\n\nBUG DESCRIPTION: Axe violation: meta-viewport\nBUG DETAILS: Ensure <meta name=\"viewport\"> does not disable text scaling and zooming. Found in 1 element(s).\nTARGET FILE: utils/ai-accessibility-helper.js\n\nCURRENT CODE:\n```\n\n```\n\nREQUIREMENTS:\n1. Generate a complete fix for this bug\n2. Fix the actual code that has the bug\n3. Return the FULL fixed file content, not just a patch\n4. Ensure the fix resolves the issue described\n5. Maintain code q...",
  "model_used": "gemini:gemini-2.5-flash",
  "raw_response": "(function() {\n    function ensureAccessibleViewport() {\n        let viewportMeta = document.querySelector('meta[name=\"viewport\"]');\n\n        // If no viewport meta tag exists, create one\n        if (!viewportMeta) {\n            viewportMeta = document.createElement('meta');\n            viewportMeta.setAttribute('name', 'viewport');\n            document.head.appendChild(viewportMeta);\n        }\n\n        let currentContent = viewportMeta.getAttribute('content') || '';\n\n        // Check if the current content explicitly disables user scaling or sets a restrictive maximum-scale.\n        // A restrictive maximum-scale is typically 1.0 or less.\n        const hasUserScalableNo = /user-scalable=no/i.test(currentContent);\n        const hasRestrictiveMaxScale = /maximum-scale=(1(\\.0)?|0(\\.\\d+)?)/i.test(currentContent);\n\n        // The most accessible and standard viewport setting is 'width=device-width, initial-scale=1.0'.\n        // This implicitly allows zooming and scaling.\n        const accessibleContent = 'width=device-width, initial",
  "processed_code": "(function() {\n    function ensureAccessibleViewport() {\n        let viewportMeta = document.querySelector('meta[name=\"viewport\"]');\n\n        // If no viewport meta tag exists, create one\n        if (!viewportMeta) {\n            viewportMeta = document.createElement('meta');\n            viewportMeta.setAttribute('name', 'viewport');\n            document.head.appendChild(viewportMeta);\n        }\n\n        let currentContent = viewportMeta.getAttribute('content') || '';\n\n        // Check if the current content explicitly disables user scaling or sets a restrictive maximum-scale.\n        // A restrictive maximum-scale is typically 1.0 or less.\n        const hasUserScalableNo = /user-scalable=no/i.test(currentContent);\n        const hasRestrictiveMaxScale = /maximum-scale=(1(\\.0)?|0(\\.\\d+)?)/i.test(currentContent);\n\n        // The most accessible and standard viewport setting is 'width=device-width, initial-scale=1.0'.\n        // This implicitly allows zooming and scaling.\n        const accessibleContent = 'width=device-width, initial",
  "code_length": 1044,
  "has_markdown": false
}